# git-owl

## Rules

- Always run `go test ./...` before marking work as complete. Do not tell the user something is done until tests pass.
- If tests fail, fix the issue and re-run tests before reporting completion.
