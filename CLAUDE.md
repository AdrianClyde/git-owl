# git-owl

## Rules

- Always run `go build ./...` and `go test ./...` before marking work as complete. Do not tell the user something is done until both pass.
- If the build or tests fail, fix the issue and re-run before reporting completion.
